import { getRandomQuote } from "../../randomQuotes";

describe("getRandomQuote", () => {
  it("should return a quote with content and author", () => {
    // fill the test here
  });

  it("should return a random quote from the predefined list", () => {
    // fill the test here
  });
});
