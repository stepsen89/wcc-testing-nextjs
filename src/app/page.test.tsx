// __tests__/Calculator.test.tsx
import React from "react";
import { render, screen, fireEvent } from "@testing-library/react";
import Calculator from "./page";

describe("Calculator", () => {
  test("renders correctly with initial display of 0", () => {});

  test("updates the display when numbers and operators are clicked", () => {});

  test("invalid expression", () => {});

  test("performs addition correctly", () => {});

  test("performs division correctly", () => {});

  test('shows "Infinity" when dividing through 0', () => {});

  test("clears the display when C is clicked", () => {});
});
